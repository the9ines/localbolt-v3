{
  "name": "localbolt",
  "version": "3.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspace=packages/localbolt-web",
    "dev": "npm run dev --workspace=packages/localbolt-web",
    "build": "npm run build --workspace=packages/bolt-core-browser && npm run build --workspace=packages/localbolt-browser && npm run build --workspace=packages/localbolt-core && npm run build --workspace=packages/localbolt-web",
    "typecheck": "npm run typecheck --workspace=packages/localbolt-web"
  },
  "dependencies": {
    "@the9ines/bolt-core": "^0.6.4"
  }
}
