# STATE (Retired 2026-07-03)

Hand-written state snapshots go stale by nature — this file's last snapshot is
preserved in git history. Under the ecosystem Governance OS, state is derived:

- **Current state (all repos):** generate the dashboard at the ecosystem workspace
  root — `os/bin/status.sh` → `os/DASHBOARD.md`
- **Intent:** `bolt-ecosystem/os/NOW.md`
- **History:** this repo's `docs/CHANGELOG.md` (append-only, still active) and
  `bolt-ecosystem/os/log/journal.md`
- **Routing:** `bolt-ecosystem/os/rules/doc-routing.md`

Do not add state claims here.
