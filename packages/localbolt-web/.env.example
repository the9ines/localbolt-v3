# Cloud signaling server (REQUIRED for internet peer discovery)
# If unset, cloud signaling is disabled — local-only mode.
# VITE_SIGNAL_URL=wss://your-signal-server.example.com

# Local signaling server (optional, defaults to ws://<hostname>:3001)
# VITE_LOCAL_SIGNAL_URL=ws://localhost:3001
